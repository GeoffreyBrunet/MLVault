## LLVM
```dataview
table tags as "Tags"
from "2. C++"
WHERE contains(tags, "llvm")
SORT file.name ASC
```
## CMake
```dataview
table tags as "Tags"
from "2. C++"
WHERE contains(tags, "cmake")
SORT file.name ASC
```
