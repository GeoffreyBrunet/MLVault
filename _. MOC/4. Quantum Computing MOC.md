# Maths
```dataview
table tags as "Tags"
from "4. Quantum Computing"
WHERE contains(tags, "maths")
SORT file.name ASC
```
# Gates
```dataview
table tags as "Tags"
from "4. Quantum Computing"
WHERE contains(tags, "gates")
SORT file.name ASC
```
